server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}

# server {
#     listen 443;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location / {
#          try_files $uri /index.html;
#     }
# }
